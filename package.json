{
  "name": "codefi-shared-frontend",
  "version": "0.0.0",
  "description": "Codefi monorepo",
  "main": "index.js",
  "repository": "https://github.com/ConsenSys/codefi-shared-frontend.git",
  "author": "ConsenSys",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/react": "^16.9.19",
    "lerna": "^3.20.2"
  },
  "scripts": {
    "story": "lerna run story --stream",
    "prestart": "lerna run libbuild --stream",
    "start": "lerna run start --stream"
  },
  "dependencies": {
    "@types/react-dom": "^16.9.5"
  }
}
